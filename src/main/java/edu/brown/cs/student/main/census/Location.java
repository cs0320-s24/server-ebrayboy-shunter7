package edu.brown.cs.student.main.census;

public record Location(String state, String county) {
  //  public Location {
  //    if(!Location.isValidGeolocation(lat, lon)) {
  //      throw new IllegalArgumentException("Invalid geolocation");
  //    }
  //  }
  //
  //  private isValidLocation(state, county) {
  //
  //  }
}
